[pytest]
testpaths = tests
# Short tracebacks (failing line + assertion message only, not the whole
# function body up to it) -- several tests (tests_integration/ especially)
# craft a specific, human-readable assertion message naming exactly which
# condition failed; the default long traceback buries that message under
# the entire source of often-long test functions.
addopts = --tb=short
