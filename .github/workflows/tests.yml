name: Tests

on:
  push:
    branches: [dev, main]
  pull_request:
    branches: [dev, main]

jobs:
  routes:
    name: routes
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run tests
        run: python -m pytest tests/test_routes.py -v --junitxml=test-results.xml
      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: routes tests
          path: test-results.xml
          reporter: java-junit
          fail-on-error: false

  config-loading:
    name: config-loading
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          # pandas==2.2.1 (pinned in requirements.txt) has no prebuilt wheel
          # for cp313 — pip would build it from source, which is slow even
          # on x86_64. 3.12 has wheels for every pin in requirements.txt.
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run tests
        run: python -m pytest tests/test_config_loading.py -v --junitxml=test-results.xml
      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: config-loading tests
          path: test-results.xml
          reporter: java-junit
          fail-on-error: false

  task-scheduling-base:
    name: task-scheduling-base
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run tests
        run: python -m pytest tests/test_task_manager.py tests/test_system_task.py -v --junitxml=test-results.xml
      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: task-scheduling-base tests
          path: test-results.xml
          reporter: java-junit
          fail-on-error: false

  system-task-manager:
    name: system-task-manager
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run tests
        run: python -m pytest tests/test_system_task_manager.py -v --junitxml=test-results.xml
      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: system-task-manager tests
          path: test-results.xml
          reporter: java-junit
          fail-on-error: false

  participant-manager:
    name: participant-manager
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run tests
        run: python -m pytest tests/test_participant_manager.py -v --junitxml=test-results.xml
      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: participant-manager tests
          path: test-results.xml
          reporter: java-junit
          fail-on-error: false

  system-tasks:
    name: system-tasks
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run tests
        run: python -m pytest tests/test_run_r_script.py -v --junitxml=test-results.xml
      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: system-tasks tests
          path: test-results.xml
          reporter: java-junit
          fail-on-error: false

  system-checks:
    name: system-checks
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run tests
        run: python -m pytest tests/test_check_system.py -v --junitxml=test-results.xml
      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: system-checks tests
          path: test-results.xml
          reporter: java-junit
          fail-on-error: false

  helper:
    name: helper
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run tests
        run: python -m pytest tests/test_helper.py -v --junitxml=test-results.xml
      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: helper tests
          path: test-results.xml
          reporter: java-junit
          fail-on-error: false

  interface-keyboard:
    name: interface-keyboard
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run tests
        run: python -m pytest tests_interface/test_keyboard.py -v --junitxml=test-results.xml
      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: interface-keyboard tests
          path: test-results.xml
          reporter: java-junit
          fail-on-error: false

  interface-menu-core:
    name: interface-menu-core
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run tests
        run: python -m pytest tests_interface/test_menu_navigation.py tests_interface/test_menu_display.py tests_interface/test_commands.py tests_interface/test_menu_helper.py -v --junitxml=test-results.xml
      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: interface-menu-core tests
          path: test-results.xml
          reporter: java-junit
          fail-on-error: false

  interface-display:
    name: interface-display
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run tests
        run: python -m pytest tests_interface/test_display.py tests_interface/test_terminal_prompt.py -v --junitxml=test-results.xml
      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: interface-display tests
          path: test-results.xml
          reporter: java-junit
          fail-on-error: false

  interface-participants-tasks:
    name: interface-participants-tasks
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run tests
        run: python -m pytest tests_interface/test_participant_menus.py tests_interface/test_task_menus.py -v --junitxml=test-results.xml
      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: interface-participants-tasks tests
          path: test-results.xml
          reporter: java-junit
          fail-on-error: false

  interface-settings-system:
    name: interface-settings-system
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run tests
        run: python -m pytest tests_interface/test_settings_menu.py tests_interface/test_log_menu.py tests_interface/test_system_check_menu.py tests_interface/test_main_menu.py tests_interface/test_shutdown_menu.py -v --junitxml=test-results.xml
      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: interface-settings-system tests
          path: test-results.xml
          reporter: java-junit
          fail-on-error: false
